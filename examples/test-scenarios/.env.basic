# Basic Environment Variables Test File
# ======================================
# Simple test cases for basic functionality

# Basic key-value pairs
BASIC_VAR=basic_value
SIMPLE_KEY=simple_value
TEST_VARIABLE=test_content

# Variables with numbers
VERSION=1.0.0
PORT=3000
MAX_CONNECTIONS=100

# Boolean-like values
DEBUG=true
ENABLED=false
ACTIVE=yes
DISABLED=no

# Empty values
EMPTY_VAR=
NULL_VALUE=

# Variables with underscores
USER_NAME=testuser
API_KEY=test_api_key
DATABASE_URL=localhost:5432

# Variables with mixed case
CamelCase=camelValue
UPPER_CASE=UPPER_VALUE
lower_case=lower_value
